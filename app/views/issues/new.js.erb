<% issue ||= @issue %>
<% existing_issues ||= @existing_issues %>
/*alert("!");*/
<% if existing_issues %>
	alert("record exists");
<% end %>
$('#new_record_wrapper').html("<%= escape_javascript(render(:partial => 'new', :locals => {:issue => issue, :existing_issues => existing_issues})) %>");
/*$('#new_record_wrapper').scroll();*/
