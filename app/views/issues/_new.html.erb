<%= form_for issue, :url => account_project_issues_url(@account, @project, :format => :js), :format => :js, :remote => true, :html => {"data-type" => :js, :method => :post} do |f| %>
  <%= render 'form', :f=> f, :issue => issue %>
<% end %>
