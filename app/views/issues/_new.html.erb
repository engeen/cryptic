<%= form_for issue, :url => account_project_issues_url(@account, @project, :format => :js),  :remote => true, :html => { :method => :post} do |f| %>
  <%= render 'form', :f=> f, :issue => issue %>
<% end %>
