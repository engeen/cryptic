<% title "New Project" %>


<%= form_for @project, :url => account_projects_path(@account) do |f| %>
  <%= render 'form', :f => f %>
<% end %>

<p><%= link_to "Back to List", account_projects_path(@account) %></p>
